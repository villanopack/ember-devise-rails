!!!
%html
  %head
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
      %title= content_for?(:title) ? yield(:title) : "Ember Devise Rails"
      %meta{:content => content_for?(:description) ? yield(:description) : "Ember Devise Rails", :name => "description"}
        = javascript_include_tag "application"
        = csrf_meta_tags
        = yield(:head)
  %body{:class => "#{controller_name} #{action_name}"}
    #ember-app{:role => "main"}
      = yield
  %footer
    / ! end of #container
